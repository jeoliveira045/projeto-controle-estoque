import { ProductItemDirectiveDirective } from './product-item-directive.directive';

describe('ProductItemDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductItemDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
